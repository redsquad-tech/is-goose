# Scripts

This distro keeps only the scripts required by the base CI checks.

## Available

- `check-no-native-tls.sh` - blocks disallowed native TLS crates in dependency graph.
- `check-openapi-schema.sh` - verifies generated OpenAPI artifacts are up to date.
